import { SidebarjsToggleDirective } from './sidebarjs-toggle.directive';

describe('SidebarjsToggleDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarjsToggleDirective();
    expect(directive).toBeTruthy();
  });
});
